---
# This is a template for a secret file.
# Use the following command to encrypt it before using in real environments:
# ansible-vault encrypt secrets.yml
#
# Example values (replace with real secrets)
db_password: "ChangeMe123!"
api_token: "demo-token-123"
